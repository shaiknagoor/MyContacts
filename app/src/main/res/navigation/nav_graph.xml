<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/ContactFragment">

    <fragment
        android:id="@+id/ContactFragment"
        android:name="com.live.mycontacts.ui.ContactsFragment"
        android:label="@string/contact_fragment_label"
        tools:layout="@layout/fragment_contacts">

        <action
            android:id="@+id/action_ContactFragment_to_AddContactFragment"
            app:destination="@id/AddContactFragment" />
    </fragment>
    <fragment
        android:id="@+id/AddContactFragment"
        android:name="com.live.mycontacts.ui.AddContactFragment"
        android:label="@string/add_contact_fragment_label"
        tools:layout="@layout/add_contact_fragment" />
</navigation>